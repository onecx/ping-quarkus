name: "codeQL"

on:
  push:
    branches:
      - 'main'
      - 'fix/[0-9]+.[0-9]+.x'
  pull_request:
    branches:
      - 'main'
      - 'fix/[0-9]+.[0-9]+.x'
  schedule:
    - cron: '37 21 * * 3'

jobs:
  quarkus:
    uses: onecx/ci-quarkus/.github/workflows/quarkus-codeql.yml@v1
    secrets: inherit
